-- Color palette for kaki.nvim. Any color addition or change should be made here only.
return {
  -- Base colors
  bg = "#16161a",      -- easy-on-the-eyes dark gray, lifted slightly from pure black
  bg_gray = "#1f2026", -- one step lighter than bg (for code blocks and the Telescope prompt)
  fg = "#d4d4d4",      -- main text color (a slightly softened white)
  comment = "#6b727f", -- comments (an unobtrusive gray)
  border = "#2e323b",  -- window borders

  -- The four carefully chosen accent colors
  blue = "#6da5d6",    -- desaturated blue; the only cool color, used to make the orange stand out
  green = "#9dbf7b",   -- calm, yellow-tinged green like persimmon leaves (keeps strings from shouting)
  yellow = "#e5c05e",  -- restrained mustard yellow
  orange = "#f08c3a",  -- the vivid main accent, the color of the persimmon fruit

  -- System colors
  red = "#e86464",
}
